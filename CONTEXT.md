# Excel 处理器上下文

这个上下文描述应用在处理 Excel、导出结果与排查问题时使用的核心术语，避免把不同类型的落盘内容混在一起。

## Language

**输出文件**:
应用在处理过程中被修改后保存的文件，或新生成的导出文件。
_Avoid_: 结果, 文件, 成果物

**日志文件**:
应用运行时自动写出的排障文件，例如 `运行日志.log` 和 `启动错误.log`。
_Avoid_: 记录, 控制台输出

**最近输出**:
当前会话中最近一次被应用修改或生成的输出文件，用于给用户快速打开结果所在目录。
_Avoid_: 最新文件, 最近日志
